describe("Authentication", () => {
  // it("should receive jWT token when authenticated with valid crendentials", () => {});
  it("should sum two numbers", () => {
    const x = 2;
    const y = 4;

    const sum = x + y;

    expect(sum).toBe(6);
  });
});
